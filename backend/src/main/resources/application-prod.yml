spring:
  application:
    name: digital-frames-shop

  datasource:
    url: ${DATABASE_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: false
    locations: classpath:db/migration
    baseline-on-migrate: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

  servlet:
    multipart:
      enabled: true
      max-file-size: 500MB
      max-request-size: 500MB
      file-size-threshold: 2KB

  security:
    jwt:
      secret: ${JWT_SECRET}
      expiration: 3600000
      refresh-expiration: 604800000

razorpay:
  key-id: ${RAZORPAY_KEY_ID}
  key-secret: ${RAZORPAY_KEY_SECRET}
  webhook-secret: ${RAZORPAY_WEBHOOK_SECRET}

server:
  port: ${PORT:8080}
  forward-headers-strategy: native  # Enable processing of X-Forwarded-* headers from nginx
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/xml,text/plain
    min-response-size: 1024
  error:
    include-message: always
    include-binding-errors: always
  servlet:
    session:
      timeout: 7d          # 7 days session timeout
      cookie:
        name: SESSIONID
        http-only: true
        secure: true
        same-site: none
        max-age: 604800    # 7 days in seconds (7 * 24 * 60 * 60)

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
      base-path: /actuator
  health:
    mail:
      enabled: false

logging:
  level:
    com.digitalframes: INFO
    org.springframework.security: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

cors:
  allowed-origins: ${FRONTEND_URL}
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  allow-credentials: true

app:
  frontend:
    url: ${FRONTEND_URL}
  email:
    enabled: true

video:
  upload:
    directory: /var/www/videos/cta-videos

shiprocket:
  email: ${SHIPROCKET_EMAIL:}
  password: ${SHIPROCKET_PASSWORD:}
  pickup-location: ${SHIPROCKET_PICKUP_LOCATION:Primary}
  channel-id: ${SHIPROCKET_CHANNEL_ID:}
  api:
    base-url: https://apiv2.shiprocket.in/v1/external

twilio:
  account:
    sid: ${TWILIO_ACCOUNT_SID:}
  auth:
    token: ${TWILIO_AUTH_TOKEN:}
  phone:
    number: ${TWILIO_PHONE_NUMBER:}
