# MySQL Profile Configuration
# This profile extends the default configuration

spring:
  # Email configuration - MUST use working credentials
  mail:
    username: ${MAIL_USERNAME:urbandec.in@gmail.com}
    password: ${MAIL_PASSWORD:zjij kiuf yxfl gypd}

# All other settings inherited from application.yml